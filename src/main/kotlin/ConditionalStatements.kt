fun main() {
    var a = 10
    var b = 5

    var result = if (a > b ) "big" else "small"
    println(result)

    /*
     * equivalent in java
     *
     * var result = a > b ? "big" else "small"
     *
     */
}