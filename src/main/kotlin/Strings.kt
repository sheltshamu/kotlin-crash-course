fun main(){
    val name = "Shelton"
    println(name)
    println(name.length)
    println(name.toUpperCase())
    println(name.toLowerCase())
    println(name.isEmpty())
    println(name.isNotEmpty())
}